{% extends 'base.html.twig' %}

{% block title %}profil{% endblock %}

{% block body %}

<div class="container">

    <div class="row">
        <div class="col-md-3">&nbsp;</div>
        <div class="col-md-6">
            <div class="bg-primary p-3">
                <h1>Modification du profil!</h1>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <p class="text-center">
                        <button type="submit" class="btn btn-lg btn-primary">Modifier les informations</button>
                    </p>
                    {{ form_end(form) }}
            </div>
        </div>

    </div>
</div>
{% endblock %}