{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}

<div class="container">

    <div class="row">
        <div class="col-md-3">&nbsp;</div>
        <div class="col-md-6">
            <div class="bg-primary p-3">
                <h1>Inscrivez-vous !</h1>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <p class="text-center">
                        <input type="submit" value="Je m'inscris" class="btn btn-lg btn-primary">
                    </p>
                    {{ form_end(form) }}
            </div>
        </div>

    </div>
</div>
{% endblock %}